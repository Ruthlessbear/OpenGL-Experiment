#version 330 core		// 3.30版本
layout(location = 0) in vec3 position;	// 三个浮点数vector向量表示位置 position变量名
layout(location = 1) in vec3 color;
layout(location = 2) in vec3 normal;

uniform mat4 Transform_1;
uniform mat4 ProjectionMatrix;
uniform mat4 ViewMatrix;
uniform mat4 NormalToWorldMt;
void main()
{
	gl_Position = ProjectionMatrix * ViewMatrix * Transform_1 * vec4(position, 1.0f);
}